---
declared-services:
  VehicleAssistant:
    label: conversation
    plan: free
applications:
- name: VehicleAssistant
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - VehicleAssistant
  env:
    NPM_CONFIG_PRODUCTION: false